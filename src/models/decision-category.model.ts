import { MetadataEntity } from "./metadata-entity.model";

/**
 * Represents a specific category of a decision to be rated.
 * For instance, for a Real Estate purchase decision, category "Price".
 * Another example could be a "Safety" category
 * @extends MetadataEntity
 */
export class DecisionCategory extends MetadataEntity {}
